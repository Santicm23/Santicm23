---
import ThemeToggle from "../buttons/ThemeToggleButton.astro"
import Title from "../text/Title.astro"
import MenuItem from "./MenuItem.astro"

const menu = [
	{ name: "Home", href: "#home", selected: true },
	{ name: "About", href: "#about", selected: false },
	{ name: "Projects", href: "#projects", selected: false },
	{ name: "Contact", href: "#contact", selected: false },
]
---

<>
	<header
		id="menu"
		class="fixed z-20 h-16 w-full animate-slide-in-top bg-slate-50/60 backdrop-blur-xl dark:bg-gray-950/60"
	>
		<nav class="m-auto flex h-full w-2/3 items-center justify-between px-1 md:gap-0">
			<Title />
			<ul class="hidden w-1/2 items-center gap-4 self-center px-4 sm:flex">
				{
					menu.map((data) => (
						<li class="group relative flex flex-col items-center">
							<MenuItem
								{...data}
								onSelect={(name: string) => {
									menu.forEach((item) => {
										item.selected = item.name === name
									})
								}}
							/>
						</li>
					))
				}
			</ul>
			<ThemeToggle class="flex justify-end" />
		</nav>
	</header>
	<div class="h-16 bg-slate-50 dark:bg-gray-950"></div>
	<hr class="border-slate-200 dark:border-slate-800" />
</>
